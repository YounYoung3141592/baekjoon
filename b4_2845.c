#include <stdio.h>

int main()
{
    int peoperPerMeterSquare, partyArea;
    int paperNumber[5];
    scanf("%d %d", &peoperPerMeterSquare, &partyArea);
    for (int i = 0; i < 5; i++)
    {
        }
}